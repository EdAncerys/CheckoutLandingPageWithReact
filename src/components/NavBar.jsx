import React from 'react';

export default function NavBar({ props }) {
  return (
    <div style={styles.container}>
      <div>Checkout</div>
    </div>
  );
}

const styles = {
  container: {},
};
